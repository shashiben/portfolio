import 'package:flutter/material.dart';

class ColorConfigs {
  static const Color primaryColor = Color(0xFF00f6ff);
  static const Color backgroundColor = Color(0xFF010510);
  static const Color textSecondaryColor = Color.fromRGBO(255, 255, 255, 0.7);
}
