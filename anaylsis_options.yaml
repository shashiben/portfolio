include: package:flutter_lints/flutter.yaml
analyzer:
  exclude:
    - "**/*.g.dart"

linter:
  rules:
    - avoid_empty_else
    - avoid_relative_lib_imports
    - avoid_print
    - prefer_single_quotes
